export type GridLoadingStateProps = {
  /** Number of court columns to render skeletons for. */
  columnCount?: number;
  /** Number of time-slot rows to render skeletons for. */
  rowCount?: number;
};
