# Postgres connection string used by Prisma (prisma.config.ts, lib/prisma.ts).
DATABASE_URL=

# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SIGNING_SECRET=

NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# Mercado Pago (external prerequisite — NOT authenticated in this dev environment;
# mirrors the DATABASE_URL external-prerequisite pattern from the auth change.
# Live preference creation / webhook delivery cannot be verified until these
# are provisioned against a real Mercado Pago account).
MERCADOPAGO_ACCESS_TOKEN=
MERCADOPAGO_WEBHOOK_SECRET=
